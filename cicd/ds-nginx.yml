#apiVersion: apps/v1
#kind: DaemonSet
#metadata:
  #name: my-nginx-ds
#spec:
#  selector:
#    matchLabels:
#      run: my-nginx-ds

 # updateStrategy:
 #   type: OnDelete
 # template:
 #   metadata:
  #    labels:
  #      run: my-nginx-ds
  #  spec:
  #    containers:
  #    - name: my-nginx-pod-ds
  #      image: nginx
#        command: ["/docker-entrypoint.sh"]
#        args: ["nginx", "-g", "daemon off;"]
#        command: ["echo"]
#        args: ["123"]
 #       ports:
 #       - containerPort: 80
#          hostPort: 32000
#        volumeMounts:
#        - mountPath: /cache
#          name: cache-v
#      volumes:
#      - name: cache-v
#        persistentVolumeClaim:
#         claimName: nfs
